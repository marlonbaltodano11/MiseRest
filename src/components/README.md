# Components Directory

Contains reusable components across the application.

- Use `PascalCase.tsx` for component files.
- Prefer keeping components generic; specific components for a route should go under `app/<route>/components/`.
